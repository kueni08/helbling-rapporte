Letterhead image for the Installationsrapport print format.

Place the Helbling & Co. AG letterhead image here as:
  letterhead.png

The image will automatically be embedded (base64) in the HTML rapport
header for both email sending and browser print.

Recommended: Use the official Helbling letterhead image (2119x223 px).
